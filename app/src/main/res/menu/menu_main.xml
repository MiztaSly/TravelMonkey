<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/save_menu"
        android:icon="@drawable/ic_save_action"
        android:title="@string/save_item_menu"
        app:showAsAction="ifRoom"></item>

    <item android:id="@+id/delete_menu"
        android:title="@string/delete_deal"
        android:icon="@drawable/ic_delete_forever_action"
        app:showAsAction="always"></item>
</menu>